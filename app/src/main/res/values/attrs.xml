<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- This is declaring the XML attributes that can be set within a theme. -->
    <!-- Check the "res/values/themes.xml" file for examples of these attributes being set.  -->

    <!-- =============================================================== -->
    <!-- Personalization, SignUp, LogIn Styles - Stays Consistent -->
    <!-- =============================================================== -->

    <attr name="pageBackground" format="reference"/>
    <attr name="button" format="reference"/>
    <attr name="editBox" format="reference"/>
    <attr name="textView" format="reference"/>

    <!-- =============================================================== -->
    <!-- Needs to Switch Based on Theme Selected -->
    <!-- =============================================================== -->

    <attr name="subBackground" format="reference"/>
    <attr name="mainBackground" format="reference"/>
    <attr name="textSecondaryHeaders" format="reference"/>
    <attr name="textPersonalizedListHeaders" format="reference"/>
    <attr name="textRegular" format="reference"/>
    <attr name="chatBubbleOtherUser" format="reference"/>
    <attr name="chatSubheaders" format="reference"/>
    <attr name="awesomeCount" format="reference"/>
    <attr name="postDate" format="reference"/>
    <attr name="smallTextDate" format="reference"/>
    <attr name="prettyDivider" format="reference"/>
    <attr name="navDrawer" format="reference"/>
    <!-- dialogs? -->

</resources>